{
  "name": "sportscal-inbound-mail-worker",
  "version": "1.0.0",
  "description": "Cloudflare Email Worker that parses inbound mail and forwards to the SportsCal backend",
  "private": true,
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev":    "wrangler dev",
    "tail":   "wrangler tail"
  },
  "dependencies": {
    "postal-mime": "^2.4.4"
  },
  "devDependencies": {
    "wrangler": "^3.114.0"
  }
}
